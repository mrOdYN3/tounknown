Bottom sheet for Path details, the practice player, the Dīkṣā gate, and the dāna moment.

```jsx
<Sheet open={show} onClose={()=>setShow(false)} ariaLabel="Path details">…</Sheet>
```

Scrim click and Esc both close; content scrolls inside; 28px top radius with grabber.
