Donation amount chips, single row, gold selected state. Pair with a "Give with a free heart" primary button; dāna appears AFTER practice, never before.

```jsx
<DanaChips onSelect={setAmount} />
<DanaChips amounts={[5,11,22,54]} selected={11} />
```
