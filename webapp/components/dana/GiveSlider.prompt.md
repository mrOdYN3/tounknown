The membership "give more if it feels true" slider — sits above the pay button in the Student tier card.

```jsx
<GiveSlider min={11} max={33} onChange={setAmount} />
```

Base is the minimum; it only raises, never pressures.
